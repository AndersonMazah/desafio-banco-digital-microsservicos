version: "3.8"

services:
  postgres:
    image: postgres:13
    container_name: postgres-desafio
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: desafio
    ports:
      - "5432:5432"
    volumes:
      - ~/.AppData/Docker/pgsql-desafio:/var/lib/postgresql/data


